---
name: "EntraID ci"

on:
  push:
    branches:
      - main
    tags:
      - "v*"
    paths:
      - "entra-id-backend/**"
      - ".github/workflows/entra-id-backend-ci.yml"
      - ".github/workflows/template-build-test-publish.yml"
  pull_request:
    paths:
      - "entra-id-backend/**"
      - ".github/workflows/entra-id-backend-ci.yml"
      - ".github/workflows/template-build-test-publish.yml"

permissions: {}

jobs:
  call-shared-workflow:
    uses: ./.github/workflows/template-build-test-publish.yml
    with:
      module: "entra-id-backend"
    secrets:
      DOCKERHUB_USERNAME: ${{ secrets.DOCKERHUB_USERNAME }}
      DOCKERHUB_TOKEN: ${{ secrets.DOCKERHUB_TOKEN }}
    permissions:
      security-events: write # Required to upload SARIF reports
      pull-requests: write # Required to post comments on PRs
