---
name: "Notion Mongo CI/CD"

on:
  push:
    branches:
      - main
    tags:
      - "v*"
    paths:
      - "notion-mongo-demo/**"
      - ".github/workflows/notion-mongo-ci.yml"
      - ".github/workflows/template-build-test-publish.yml"
  pull_request:
    paths:
      - "notion-mongo-demo/**"
      - ".github/workflows/notion-mongo-ci.yml"
      - ".github/workflows/template-build-test-publish.yml"

permissions: {}

jobs:
  call-shared-workflow:
    uses: ./.github/workflows/template-build-test-publish.yml
    with:
      module: "notion-mongo-demo"
    secrets:
      DOCKERHUB_USERNAME: ${{ secrets.DOCKERHUB_USERNAME }}
      DOCKERHUB_TOKEN: ${{ secrets.DOCKERHUB_TOKEN }}
    permissions:
      security-events: write # Required to upload SARIF reports
      pull-requests: write # Required to post comments on PRs
